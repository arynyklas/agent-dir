# My .agent dir for AI tools

## Installation

```bash
git clone git@github.com:arynyklas/agent-dir ~/.agent
```

Addtionally install _agentsys_ with selection for Codex CLI:

```bash
npm install -g agentsys && agentsys
```

> _requires Node v24_
